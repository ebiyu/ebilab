# TODO.md

- [x] `samples` を新APIへ更新する
- [x] 実験が終了しても実験ごとのログファイルが閉じられない問題を修正
- [x] ログビューに `logger.exception` 時の例外の内容が表示されないバグを修正
- [x] 実験がエラー終了したらエラーダイアログで通知するようにする。
- [x] ログビューの改善。
    - TreeViewにして見やすく。
    - ログレベルでフィルタできるようにする。default: INFO
    - exp.logger からのログだけを表示するようにもできるようにする。default: on
- [x] Plotterから `Experiment` クラスにアクセスできるように。
- [x] 実験スレッドが `await` 中でも中止できるように。(`sample/sleep.py`)
- [x] 過去データ表示機能の中身を実装。データフォルダのcsvを読み込んで表示する。
    - そのためにはcsvファイルのメタデータを保存する機能が必要になりそう。
- [x] 最後に "sync" をしたタイミングからの経過時間を保持する `sync_t` 列を、 `t` に加えて追加。
- [x] 過去データにコメントを付ける機能を実装。コメントの保存が必要。
- [x] デバッグ実行中は、デバッグ実行でありデータが保存されていないことを示す警告を画面上に表示して。
- [x] ~~デバッグモードではログファイルも保存しなくてよいです。~~
    - 実験中にデバッグモードを切り替えられるようにしたので撤回。ログは実験開始時に
      常に作る（装置を暖めている間のログも残したいため）。CSVは記録中のみ。
- [ ] Protocol / Plotter の選択・パラメータ編集の有効・無効（実験中はPlotterのみ変更できるようにするなど）
- [x] 長時間実験するとPlotterが重くなる問題をなんとかする。
    - window (`window_length`) 機能を実装。末尾N行のみを `update()` に渡す。
- [x] 実験を走らせたまま、記録（CSVへの保存）だけを任意のタイミングで開始・終了できるように
  する。装置を安定させてから測りたい実験のため。
    - 実験中に `ExperimentService.set_debug_mode()` を呼ぶと、その時点で記録が始まる／
      終わる。デバッグ実行で開始しておけば、装置が安定してから記録を始められる。
    - GUIでは実験中の開始ボタン(F5)が記録開始、デバッグボタン(F6)が記録終了として働く。
      ボタンもキー割り当ても増やしていない。押せるほうだけを有効にする。
    - 1回の実験実行で、CSVは記録ごとに1本作られる。ログは記録の有無に関わらず実験ごとに
      1本作られ、記録の開始・終了が `[recording]` として残る。
    - `t` は実験開始からの連続値のまま（Plotterが巻き戻らない）。記録開始が sync 扱いに
      なるので、`sync_t` が記録開始からの経過時間になる。
- [ ] 監視モード実装
